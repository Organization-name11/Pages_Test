
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",                     
    "start": "next start",
    "predeploy": "NODE_ENV=production npm run build && touch out/.nojekyll",
       "deploy": "gh-pages -d out"
  }
}